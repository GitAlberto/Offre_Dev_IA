# Dossier `queries`

Ce dossier contiendra les requetes SQL et HiveQL documentees.

On y ajoutera ensuite :
- les requetes PostgreSQL pour les preuves C2 ;
- les requetes Hive pour les agregats analytiques ;
- les commentaires expliquant l'objectif de chaque requete.

Sous-dossiers deja prepares :
- `queries/hive/` : requetes HiveQL executees contre Hive / Hue.
