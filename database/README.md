# Dossier `database`

Ce dossier regroupera tout ce qui concerne la persistance en base de donnees.

On y trouvera ensuite :
- les modeles SQLAlchemy ;
- les scripts d'import ;
- les migrations SQL ;
- les elements de structure de la base PostgreSQL.
